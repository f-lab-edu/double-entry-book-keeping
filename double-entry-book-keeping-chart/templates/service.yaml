apiVersion: v1
kind: Service
metadata:
  name: {{ include "double-entry-book-keeping-chart.fullname" . }}
  labels:
    {{- include "double-entry-book-keeping-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "double-entry-book-keeping-chart.selectorLabels" . | nindent 4 }}
